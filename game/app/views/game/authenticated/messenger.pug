extends ../layout_auth

append content
    if message
        h1=message
    h2 Advanced HTML messaging system
    form(name="message", method="post")
        div.input
            span.label To
            input(type="text", name="to")

        //TODO: HTML5 wisywig to indicate that JS is allowed
        div.input
            span.label Message -- Some html 5 wisywig to go here!
            input(type="text", name="message")
        div.actions
            input(type="submit", value="Send")